# 🏹 FastArcher

**FastArcher** — Telegram-бот для анализа и учёта тренировок по стрельбе из лука.
Позволяет загружать CSV-файлы с данными о выстрелах.

---

##  Возможности
-  Регистрация через Telegram-бота
-  Загрузка CSV-файлов с тренировочными данными:
   - `UserSummary` — сводные показатели по тренировкам пользователя, рассчитанные на основе всех загруженных сессий и выстрелов.(общее количество сессий, средний счёт, всего выстрелов.)

   - `TrainingSession` — каждая строка соответствует одной тренировочной сессии пользователя, информация о конкретной тренировочной сессии.(дата, счёт, количество выстрелов...)

   - `ShotData` — детальные данные по каждому выстрелу в рамках сессии.(метка времени, балл за выстрел...)

- Статистические отчёты через Telegram:

  -  Всего сессий и дней тренировок.
  -  Средний счёт.
  -  Всего выстрелов.
  -  Лучшая сессия и дата.
  -  Дата последней тренировки.

---

##  Структура проекта
```bash
FastArcher/
├─ app/
│  ├─ api/                 # Эндпоинты FastAPI (роуты для работы с пользователями, статистикой, загрузкой данных и т.д.)
│  ├─ bot/                 # Telegram-бот: хендлеры, меню, команды, взаимодействие с API
│  ├─ core/                # Конфигурация приложения (настройки, база данных, валидация переменных окружения)
│  ├─ daos/                # DAO — работа с базой данных через SQLAlchemy
│  ├─ docker/              # Настройки для сборки контейнеров
│  ├─ models/              # ORM-модели таблиц PostgreSQL (User, UserSummary, TrainingSession, ShotData)
│  ├─ requirements/        # requirements.txt-файл для зависимостей
│  ├─ services/            # Бизнес-логика (обработка CSV, вычисление статистики, управление пользователями)
│
├─ .env                    # Переменные окружения (доступ к БД, API, токен Telegram)
├─ docker-compose.yml      # Контейнеризация: запуск API, базы данных и бота
└─ README.md               # Документация проекта
```

## Таблица `users` — информация о пользователях

Хранит данные о зарегистрированных пользователях Telegram-бота.

- Поля:
  - id — уникальный идентификатор пользователя
  - telegram_id — ID пользователя в Telegram
  - username — имя пользователя
  - password_hash — хешированный пароль
  - is_active — активен ли пользователь
  - verification_code — код верификации
  - registered_at — дата и время регистрации пользователя

Используется как основная таблица для связи с другими сущностями: user_summary, training_session, shot_data.

## Загрузка CSV-файлов
Важно: бот не позволит загрузить файл, если имя пользователя в CSV не совпадает с зарегистрированным в вашем Telegram боте.
При загрузке данных программа автоматически определяет тип содержимого CSV-файла, и распределяет информацию по трём таблицам базы данных:

- Таблица `user_summaries`.
Этот таблица содержит сводные данные о пользователе. В ней указываются показатели, которые агрегируются по всем тренировкам.

- Столбцы:
  - id — идентификатор сводки пользователя
  - user_id — идентификатор пользователя
  - username — имя пользователя
  - sessions — количество тренировочных сессий
  - shots — общее количество выстрелов
  - average_score — средний результат
  - uploaded_at — время добавления файла

- Таблица `training_sessions`.
Этот таблица содержит данные о каждой тренировочной сессии. Каждая строка — отдельная сессия.

- Столбцы:
  - id — идентификатор тренировочной сессии
  - date — дата проведения тренировки
  - score — средний результат
  - bow_type — тип лука
  - shot_count — количество выстрелов
  - notes — комментарии тренера или стрелка
  - handedness — ведущая рука (левая/правая)
  - total_score — суммарный результат за сессию
  - total_arrows — количество стрел, использованных за тренировку
  - total_x — количество попаданий в «X» (центр мишени)
  - target_type — тип мишени
  - target_size — размер мишени
  - distance — дистанция до мишени (в метрах)
  - cant_pitch — наклон и угол удержания лука
  - ...
  - ...

- Таблица `shot_data`.
Этот таблица содержит данные о каждом отдельном выстреле в тренировочной сессии.

- Столбцы:
  - id — идентификатор выстрела
  - session_id — идентификатор тренировочной сессии
  - date — дата выстрела
  - time_stamp — время выстрела
  - stability_score — показатель устойчивости
  - split — время между выстрелами
  - setup_upward_blue — движение лука вверх
  - setup_downward_green — движение лука вниз
  - targeting_yellow — стабильность прицеливания
  - hold_orange — удержание перед выстрелом
  - cant, pitch, clicker — углы наклона и положение кликера
  - target_x, target_y — координаты попадания
  - target_score — результат выстрела
  - problem_direction — направление отклонения стрелы
  - ...
  - ...
### Как это работает

Пользователь загружает CSV-файлы через Telegram-бота.
Бот проверяет данные(первым должен быть загружен файл с сессиями, затем с выстрелами) и автоматически добавляет новые записи в базу.
Старые записи не перезаписываются — данные только дополняются.
После загрузки можно получать статистику: количество сессий, средний счет(очки), лучшие сессии, количество выстрелов, последние тренировки.

##  Технологии

- **Python 3.11+**
- **FastAPI** — backend API
- **python-telegram-bot** — Telegram-бот
- **SQLAlchemy / PostgreSQL** — база данных
- **Docker / Docker Compose** — контейнеризация и развёртывание

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/Pavel160/FastArcher.git
cd fastarcher
```

### 2. Настройка окружения

Создай файл .env

- Укажи в .env необходимые параметры:

`PostgreSQL`
  - DB_USER=
  - DB_PASSWORD=
  - DB_DB=
  - DB_HOST=
  - DB_INTERNAL_PORT=  (порт внутри контейнера)
  - DB_EXTERNAL_PORT=  (порт на хосте)

`API`
  - API_HOST=api
  - API_INTERNAL_PORT=  (порт внутри контейнера)
  - API_EXTERNAL_PORT=  (порт на хосте)

`Telegram`
  - TELEGRAM_TOKEN=


### 3. Запуск через Docker
```bash
docker compose up --build -d
```

### 4. Настройка базы данных

- Таблицы создадутся автоматически при запуске программы.


## Как пользоваться Telegram-ботом

- Запуск бота

  - Откройте Telegram и найдите бота по имени (например, FastArcherBot).
  - Нажмите Start или введите команду /start.
  - В предоставленном меню пройдите регистрацию
  - После зайдите в систему
  - Отправьте файл
  - Просмотрите статистику

## 📜 Лицензия
MIT License © 2025 FastArcher Project
